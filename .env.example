# Copy this file to .env.local and fill in real values. Never commit .env.local.

# Password for the lightweight /admin panel (edits shop price/availability).
# Leave unset to disable /admin entirely.
ADMIN_PASSWORD=
